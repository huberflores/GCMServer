#GCM runs as servlet in Tomcat, GlassFish, etc.

#Create a war
jar cvf ./gcmwar.war ./GCMModule/

#Upload it to cloud in your /webs/app
scp -i pk-name-key.pem -r ./gcmwar.war ubuntu@ec2-xxx-xxx-xxx-xxx.compute-1.amazonaws.com:/tomcatpath...

#Connect to your instance
ssh -i pk-name-key.pem ubuntu@ec2-xxx-xxx-xxx-xxx.compute-1.amazonaws.com


